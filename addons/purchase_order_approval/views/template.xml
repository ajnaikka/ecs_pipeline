<odoo>
    <data>
  <record id="lower_authority_approval_email" model="mail.template">
    <field name="name">Lower Authority Approved Email Template</field>
    <field name="model_id" ref="purchase.model_purchase_order"/>
    <field name="subject">Lower Authority Approved-RFQ {{object.name }}</field>
    <field name="email_from">{{(user.employee_id.work_email or '')}}</field>
    <field name="email_to">{{', '.join(user.employee_id.work_email for user in object.finance_manager_emails)}}</field>
      <field name="body_html">
        <![CDATA[
            <!-- Your email body HTML goes here -->
        ]]>
    </field>
</record>

        <record id="medium_authority_approval_email" model="mail.template">
    <field name="name">Medium Authority Approved Email Template</field>
    <field name="model_id" ref="purchase.model_purchase_order"/>
    <field name="subject">Medium Authority Approved-RFQ {{object.name }}</field>
    <field name="email_from">{{(user.employee_id.work_email or '')}}</field>
    <field name="email_to">{{', '.join(user.employee_id.work_email for user in object.finance_manager_emails)}}</field>
      <field name="body_html">
        <![CDATA[
            <!-- Your email body HTML goes here -->
        ]]>
    </field>
</record>

           <record id="higher_authority_approval_email" model="mail.template">
    <field name="name">Higher Authority Approved Email Template</field>
    <field name="model_id" ref="purchase.model_purchase_order"/>
    <field name="subject">Higher Authority Approved-RFQ {{object.name }}</field>
    <field name="email_from">{{(user.employee_id.work_email or '')}}</field>
    <field name="email_to">{{', '.join(user.employee_id.work_email for user in object.finance_manager_emails)}}</field>
      <field name="body_html">
        <![CDATA[
            <!-- Your email body HTML goes here -->
        ]]>
    </field>
</record>

    </data>
</odoo>