<odoo>
    <data>
   <record id="employee_raise_request_email" model="mail.template">
            <field name="name"> Raise Request Email Template</field>
            <field name="model_id" ref="infi_raise_a_request.model_raise_request"/>
            <field name="subject">Raise Request from {{object.employee_id.name}}</field>
            <field name="email_from">{{object.employee_id.work_email}}</field>
            <field name="email_to">{{object.employee_id.hr_manager_id.work_email}}</field>
            <field name="body_html">
                <![CDATA[

                ]]>
            </field>
        </record>

        <record id="employee_assign_reporting_manager_email" model="mail.template">
            <field name="name"> Assign to Reporting Manager Email Template</field>
            <field name="model_id" ref="infi_raise_a_request.model_raise_request"/>
            <field name="subject">Address the raise request of employee {{object.employee_id.name}}</field>
            <field name="email_from">{{object.employee_id.hr_manager_id.work_email}}</field>
            <field name="email_to">{{object.employee_id.parent_id.work_email}}</field>
            <field name="body_html">
                <![CDATA[

                ]]>
            </field>
        </record>

        <record id="employee_accepted_corrected_email" model="mail.template">
            <field name="name"> Accepted and Corrected Email Template</field>
            <field name="model_id" ref="infi_raise_a_request.model_raise_request"/>
            <field name="subject">Checked and Corrected the raise request of employee {{object.employee_id.name}}</field>
            <field name="email_from">{{object.employee_id.parent_id.work_email}}</field>
            <field name="email_to">{{object.employee_id.hr_manager_id.work_email}}</field>
            <field name="email_cc">{{object.employee_id.work_email}}</field>
            <field name="body_html">
                <![CDATA[

                ]]>
            </field>
        </record>

         <record id="employee_rejected_email" model="mail.template">
            <field name="name"> Rejected Email Template</field>
            <field name="model_id" ref="infi_raise_a_request.model_raise_request"/>
            <field name="subject">Rejected the raise request of employee {{object.employee_id.name}}</field>
            <field name="email_from">{{object.employee_id.parent_id.work_email}}</field>
            <field name="email_to">{{object.employee_id.hr_manager_id.work_email}}</field>
            <field name="email_cc">{{object.employee_id.work_email}}</field>
            <field name="body_html">
                <![CDATA[

                ]]>
            </field>
        </record>
    </data>
</odoo>
