<odoo>
    <data>
        <template id="portal_menu_inherited" name="All Portal menu : LMS Details"
                  inherit_id="openeducat_web.new_portal_menu"
                  priority="10">
            <xpath expr="//t/a[hasclass('dashboard_element_main_body')]/div[hasclass('hover_effect')]" position="before">
                <t t-if="'LMS' in menu.name">
                    <div class="col-lg-12 text-right px-0">
<!--                        <span class="badge badge-secondary badge-pill"-->
<!--                              t-esc="course_count"/>-->
                    </div>
                </t>
            </xpath>
        </template>
    </data>
</odoo>