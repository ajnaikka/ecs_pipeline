<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="crm_invoice_form_view" model="ir.ui.view">
            <field name="name">crm.lead.invoice.creation</field>
            <field name="model">crm.lead</field>
            <field name="inherit_id" ref="crm.crm_lead_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='action_set_won_rainbowman']" position="before">
                    <button string="Create Invoice" name="action_create_invoice" type="object" class="oe_highlight"
                            attrs="{'invisible': ['|', ('type', '=', 'lead'), '&amp;', ('probability', '=', 0), ('active', '=', False)]}"/>
                </xpath>
                <button name="action_schedule_meeting" position="after">

                    <button name="action_view_invoice"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-pencil-square-o">
                        <field name="invoice_count" widget="statinfo" string="Invoices"/>
                    </button>
                </button>

                <xpath expr="//page[@name='student_employment_details']" position="after">
                    <page name="invoice_details" string="Invoice Details">
                        <field name="invoice_line_ids">
                            <tree>
                                <field name="name"/>
                                <field name="invoice_date_due"/>
                                <field name="state"/>
                                <field name="payment_state"/>

                            </tree>

                        </field>
                    </page>
                </xpath>


            </field>
        </record>

    </data>
</odoo>

