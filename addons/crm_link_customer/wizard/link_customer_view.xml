<odoo>
    <data>
        <record id="crm_link_customer_wizard_view" model="ir.ui.view">
            <field name="name">crm.link.customer.wizard</field>
            <field name="model">crm.link.customer</field>
            <field name="arch" type="xml">
                <form string="Link to customer">
                    <group>
                        <group name="action" string="Customer"
                               col="1">
                            <field name="action" nolabel="1" widget="radio"/>
                            <group col="2">
                                <field name="partner_id" widget="res_partner_many2one"
                                       context="{'res_partner_search_mode': 'customer', 'show_vat': True}"
                                       attrs="{'required': [('action', '=', 'exist')], 'invisible':[('action','!=','exist')]}"/>
                            </group>
                        </group>
                        <group>
                            <field name="lead_id" invisible="1"/>
                        </group>
                    </group>
                    <footer>
                        <button name="action_apply" string="Link To Customer" type="object" class="btn-primary"
                                data-hotkey="q"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_crm_link_customer" model="ir.actions.act_window">
            <field name="name">Link To Customer</field>
            <field name="res_model">crm.link.customer</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="view_id" ref="crm_link_customer_wizard_view"/>
        </record>
    </data>
</odoo>